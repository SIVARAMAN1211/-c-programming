#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ifstream source("data.txt");
    ofstream dest("copy.txt");

    string line;

    while(getline(source, line))
        dest << line << endl;

    source.close();
    dest.close();
}
